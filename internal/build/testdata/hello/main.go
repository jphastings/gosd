// Command hello is a minimal main package used as a build fixture: just
// enough to compile into a real binary so tests can inspect the result.
package main

func main() {
	println("hello from gosd build testdata")
}
