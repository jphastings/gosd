// Command hello is a minimal example app used to exercise the gosd build
// pipeline end to end. A later bean will give it real behaviour (e.g.
// blinking an LED or serving a status page); for now it just runs.
package main

func main() {
	println("hello from gosd")
}
